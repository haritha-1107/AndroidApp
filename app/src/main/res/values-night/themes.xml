<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base dark theme -->
    <style name="Theme.Sssshhhift" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primary_variant</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Surface colors -->
        <item name="colorSurface">@color/profile_card_background_dark</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary_dark</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/background_primary_dark</item>
        <item name="colorOnBackground">@color/text_primary_dark</item>
        
        <!-- Error colors -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>
        
        <!-- Outline colors -->
        <item name="colorOutline">@color/profile_card_stroke_dark</item>
        <item name="strokeColor">@color/profile_card_stroke_dark</item>
        
        <!-- Material Design specific -->
        <item name="materialCardViewStyle">@style/Widget.App.CardView.Dark</item>
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView.Dark</item>
        <item name="materialButtonStyle">@style/Widget.App.Button.Dark</item>
    </style>

    <!-- Material Button Style -->
    <style name="Widget.App.Button.Dark" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/on_primary_dark</item>
        <item name="backgroundTint">@color/primary_variant</item>
        <item name="rippleColor">@color/ripple_dark</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Bottom Navigation Style -->
    <style name="Widget.App.BottomNavigationView.Dark" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/bottom_nav_item_color_dark</item>
        <item name="itemTextColor">@color/bottom_nav_item_color_dark</item>
        <item name="android:background">@color/nav_background_dark</item>
    </style>

    <!-- Card View Style -->
    <style name="Widget.App.CardView.Dark" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/card_background_dark</item>
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="strokeColor">@color/stroke_color_dark</item>
        <item name="strokeWidth">1dp</item>
    </style>
</resources>

